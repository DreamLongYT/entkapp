import { Services0Service } from '@enterprise/services';
import { Domain5Repository } from '@enterprise/domain';
const s = new Services0Service();
const d = new Domain5Repository();
s.execute(); d.execute();
